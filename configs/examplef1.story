.Include</home/dg/etc/config_i1/common.story>

# add any altered functions here
# They will overwrite common definitions

#function(bannedcheck)
#if(true,,502) return setblock
#if(urlin, banned) return setblock
#if(headerin, bannedheader) return setblock

